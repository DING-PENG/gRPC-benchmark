# gRPC-benchmark
Mini benchmark for gRPC

## Build
----------------
```bash
make
```
Change `$(HOME)/.local` to `/usr/local` if necessary.

## Run
-------------
Server side:

```bash
./server
```

Client side:

```bash
./client <server address:port>
```
